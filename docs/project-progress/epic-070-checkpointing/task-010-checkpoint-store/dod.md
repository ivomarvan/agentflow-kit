# DoD — E070 T010 CheckpointStore + InMemoryCheckpointStore

- [ ] `checkpoint.py` created with `CheckpointRecord`, `CheckpointStore`, `InMemoryCheckpointStore`.
- [ ] All 5 unit tests pass.
- [ ] `ruff check` passes.
- [ ] `mypy --strict --follow-imports=skip` passes on `checkpoint.py`.
- [ ] Full regression: `pytest src/agentflow/tests/ -v -m "not integration"`.
